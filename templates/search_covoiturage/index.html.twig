{% extends 'base.html.twig' %}

{% block title %}Rechercher un covoiturage{% endblock %}

{% block body %}
<div data-controller="search-covoiturage" class="container my-5">
    <h1 class="text-center mb-4">Rechercher un covoiturage</h1>
    <form data-action="submit->search-covoiturage#search" class="row g-3">
        <!-- Ville de départ -->
        <div class="col-md-6">
            <label for="pointDepart" class="form-label">Ville de départ</label>
            <input 
                data-search-covoiturage-target="pointDepart" 
                type="text" 
                id="pointDepart" 
                class="form-control" 
                placeholder="Exemple : Paris" 
                required 
            />
        </div>

        <!-- Ville de destination -->
        <div class="col-md-6">
            <label for="pointArrivee" class="form-label">Ville de destination</label>
            <input 
                data-search-covoiturage-target="pointArrivee" 
                type="text" 
                id="pointArrivee" 
                class="form-control" 
                placeholder="Exemple : Lyon" 
                required 
            />
        </div>

        <!-- Date de départ -->
        <div class="col-md-6">
            <label for="dateDepart" class="form-label">Date de départ</label>
            <input 
                data-search-covoiturage-target="dateDepart" 
                type="date" 
                id="dateDepart" 
                class="form-control"
            />
        </div>

        <!-- Heure de départ -->
        <div class="col-md-6">
            <label for="heureDepart" class="form-label">Heure de départ</label>
            <input 
                data-search-covoiturage-target="heureDepart" 
                type="time" 
                id="heureDepart" 
                class="form-control" 
                placeholder="Heure de départ" 
                aria-label="Heure de départ" 
            />
        </div>

        <!-- Bouton Rechercher -->
        <div class="col-12 text-center">
            <button type="submit" class="btn btn-primary px-4 py-2">Rechercher</button>
        </div>
    </form>

    <!-- Zone d'affichage des résultats -->
    <div data-search-covoiturage-target="results" class="mt-4">
        <!-- Les résultats seront affichés ici -->
    </div>

    <!-- Message "Aucun résultat" -->
    <div data-search-covoiturage-target="noResults" class="hidden alert alert-warning mt-4 text-center">
        Aucun trajet trouvé.
    </div>
</div>
{% endblock %}


